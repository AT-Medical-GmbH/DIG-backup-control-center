# Copy to an operator-managed location outside Git.
# Fill values locally and protect the file with restrictive permissions.
RESTIC_PASSWORD=<set-locally>
NTFY_TOPIC=<set-locally>
AGE_PRIVATE_KEY_FILE=/etc/atmed-bcc/keys/age.txt
